const AUTH_TOKEN_KEY = 'authorization_token'

const AUTH_TOKEN_SAVE_TIME = 'token_save_time'

const AUTH_TOKEN_EXPIRE = 'token_expire'

const ESM_USERNAME = 'esm_sys_username'

export { AUTH_TOKEN_EXPIRE, AUTH_TOKEN_KEY, AUTH_TOKEN_SAVE_TIME, ESM_USERNAME }
